---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: loadworkers.loadtest.gitopscd.ru
spec:
  group: loadtest.gitopscd.ru
  names:
    kind: LoadWorker
    listKind: LoadWorkerList
    plural: loadworkers
    singular: loadworker
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: LoadWorker is the Schema for the loadworkers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LoadWorkerSpec defines the desired state of LoadWorker.
            properties:
              childCount:
                description: ChildCount определяет количество дочерних элементов.
                minimum: 0
                type: integer
              maxConcurrent:
                description: MaxConcurrent определяет максимальное количество одновременных
                  операций.
                minimum: 1
                type: integer
              mode:
                description: Mode определяет режим работы.
                enum:
                - auto
                - manual
                type: string
              payloadSize:
                description: PayloadSize задаёт размер полезной нагрузки в байтах.
                minimum: 0
                type: integer
              statusUpdateTimeout:
                description: StatusUpdateTimeout задаёт таймаут обновления статуса
                  в секундах.
                minimum: 1
                type: integer
              updateFrequency:
                description: UpdateFrequency задаёт частоту обновления в миллисекундах.
                minimum: 1
                type: integer
            type: object
          status:
            description: LoadWorkerStatus defines the observed state of LoadWorker.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
